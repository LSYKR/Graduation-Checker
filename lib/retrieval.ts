import type { EvidenceChunk, GraduationRuleSet } from "./types";
const tokens = (value: string) => [...new Set(value.toLowerCase().replace(/[^0-9a-zA-Z가-힣\s]/g, " ").split(/\s+/).filter((token) => token.length > 1))];
export function retrieveEvidence(rule: GraduationRuleSet, query: string, limit = 3): EvidenceChunk[] { const queryTokens = tokens(query); return rule.evidence.map((chunk) => { const haystack = tokens(`${chunk.title} ${chunk.text} ${chunk.keywords.join(" ")}`); const score = queryTokens.reduce((sum, token) => sum + (haystack.some((item) => item.includes(token) || token.includes(item)) ? 2 : 0), 0) + chunk.keywords.reduce((sum, keyword) => sum + (query.includes(keyword) ? 3 : 0), 0); return { chunk, score }; }).sort((a, b) => b.score - a.score).slice(0, limit).map((item) => item.chunk); }
